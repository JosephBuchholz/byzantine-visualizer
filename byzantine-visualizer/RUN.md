# Run the Project

## Setup

Run `npm install` from this directory to install everything (npm is required).

## Run

Run `npm run dev`.

## Other

Format all the code with `npm run format`.

See the "scripts" block in `package.json` for other scripts to run.